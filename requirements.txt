# Web Framework & Async Server
fastapi>=0.111.0
starlette>=0.37.2
uvicorn[standard]>=0.30.0

# Configuration & Data Validation
pydantic>=2.7.0
pydantic-settings>=2.2.0
python-dotenv>=1.0.1

# Agent Orchestration & LLM Providers
langchain>=0.2.0
langchain-core>=0.2.0
langchain-openai>=0.1.7
langchain-google-genai>=1.0.0
langgraph>=0.1.0

# Math, Statistics & Causal Engine
numpy>=1.26.0
scipy>=1.13.0

# Database & Visuals
sqlalchemy>=2.0.0
duckdb>=1.0.0
plotly>=5.20.0

# External Telemetry & MCP
requests>=2.31.0
mcp>=1.0.0

# Databricks Apps deployment. Only needed when DEPLOY_TARGET resolves to
# databricks — every adapter imports these lazily, inside the function that
# uses them, so local development runs without them installed.
gunicorn>=22.0
databricks-sdk>=0.118.0
psycopg[binary]>=3.1.0
openai>=1.40.0
